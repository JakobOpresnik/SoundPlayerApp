type ColorScheme = {
    primary: string,
    background?: string,
    textPrimary: string,
    border: string,
    white: string,
    black: string,
}

const colors: ColorScheme = {
    primary: "#ACACE6",
    textPrimary: "#696969",
    border: "#DEDEDE",
    white: "#FFF",
    black: "#000",
}

export default colors;